# WIN-AIBCH — ระบบบันทึกการกระทำผิดระเบียบของนักเรียน โรงเรียนบาเจาะ

> ระบบจัดการความประพฤตินักเรียนตามแบบฟอร์มทางการของโรงเรียน (กจ.1, กจ.1.1, กจ.2)
> ผู้ดูแลระบบ: **ครูสารวัตร** (Admin)

---

## 1. ภาพรวมระบบ

| หัวข้อ | รายละเอียด |
|---|---|
| ผู้ใช้งาน | ครูสารวัตร (บัญชีเดียว = Admin ทำได้ทุกอย่าง) |
| แพลตฟอร์ม | Web App (PC / แท็บเล็ต / มือถือ) |
| Tech Stack | Next.js 15 (App Router) + TypeScript + Tailwind CSS + Prisma + SQLite |
| Auth | Credentials login + Session cookie |
| ธีม UI | สปอร์ต/สารวัตร — Dark theme, แถบ hazard-stripe เหลือง-แดง, ฟอนต์ Kanit, ตัวเลขสถิติแบบ scoreboard |
| เอกสารพิมพ์ | ตรงข้ามกับ UI — ขาว-ดำ ทางการ ฟอนต์ TH SarabunPSK ขนาด A4 ตามต้นฉบับ |

## 2. เวิร์กโฟลว์เคส (ตามแบบฟอร์มจริง)

```
[1] บันทึกเหตุการณ์ (Incident) — ระบบคำนวณมาตรการครั้งที่ N อัตโนมัติ
      ↓ ออกเอกสาร
[2] กจ.1  บันทึกความ — เชิญครูที่ปรึกษา + ผู้ปกครอง (ข้ามได้เมื่อมาตรการครั้งที่ 1)
      ↓
[3] กจ.1.1 แบบข้อตกลงการฝึกความประพฤติเรียนรู้
      ↓
[4] กจ.2  บันทึกความ — มาตรการแก้ไข + ความเห็นผู้อำนวยการ (บังคับ)
      ↓
[5] ลงนามอิเล็กทรอนิกส์ 4 ฝ่าย (นักเรียน/ผู้ปกครอง/ครูสารวัตร/ผอ.)
      ↓
[6] ปิดเคส — ได้เมื่อลงนามครบ 4 ฝ่ายเท่านั้น
```

สถานะเคส: `recorded → summoned (กจ.1) → agreement (กจ.1.1) → corrective (กจ.2) → signing (ลงนาม) → closed`

**มาตรการครั้งที่ 1 (ว่ากล่าวตักเตือน):** ไม่ต้องนัดผู้ปกครอง — ปุ่ม "ข้ามไป กจ.2" จากขั้นบันทึกเหตุ (stepper แสดงขั้นที่ข้ามเป็น ↷)

**ช่องเลขประจำตัวนักเรียน:** หน้าบันทึกเหตุ/หน้ารายการใช้คำว่า "เลขประจำตัวนักเรียน" — เอกสารพิมพ์คงคำว่า "เลขที่" ตามแบบฟอร์มต้นฉบับ

## 3. โครงสร้างแบบฟอร์มทางการ (ถอดจากต้นฉบับ)

### กจ.1 — บันทึกความ (เชิญผู้ปกครอง)
- ที่ (เลขที่หนังสือ) / วันที่ ..... เดือน ..... พ.ศ. .....
- เรื่อง: นักเรียนชั้นประถมศึกษาปีที่/มัธยมศึกษาปีที่ .....
- เชิญ: ครูที่ปรึกษาชั้น ม.../... และ ผู้ปกครองนักเรียน (ชื่อ-สกุล / ชั้น / เลขที่)
- "บัดนี้ ข้าพเจ้า ได้รับ/ปรากฏ/เห็น และได้รับการร้องเรียนจากท่านเป็นประจำ ตั้งแต่นักเรียนได้เข้ามาเรียนในสังกัด" + **เช็คลิสต์พฤติกรรม** (ดูหัวข้อ 4)
- เชิญมาเพื่อโปรดพิจารณาและช่วยชี้แนะแนวทางในการฝึก (ผม/นางสาว...) ตามกิจการนักเรียน 1
- กำหนดให้มาในวันที่ ..... เวลา ..... น. ที่ .....
- ลงนาม: ครูที่ปรึกษาชั้น / ผู้ช่วยผู้อำนวยการ-รองผู้อำนวยการฝ่ายกิจการนักเรียน

### กจ.1.1 — แบบข้อตกลงการฝึกความประพฤติเรียนรู้
- วันที่ / โรงเรียนบาเจาะ (สพฐ.)
- พิจารณาแล้วเห็นว่า นักเรียน ..... ชั้นมัธยมศึกษาปีที่ ..... เลขที่ ..... เป็นผู้กระทำผิดกฎระเบียบของโรงเรียน โดยมีพฤติกรรม ..... เป็นเหตุให้เสียหาย/รบกวนการเรียนการสอน
- ข้อตกลง: กำหนดให้ทำความดี (ระบุพฤติกรรม/งานที่ต้องทำ) เพื่อชดใช้ความผิด ภายในระยะเวลา ..... หากไม่ปฏิบัติตามจะพิจารณาทางวินัยต่อไป
- ลงนาม 5 ฝ่าย: นักเรียน / ผู้ปกครอง / ครูที่ปรึกษา / รอง-ผู้ช่วยผอ.ฝ่ายกจน. / ผู้อำนวยการโรงเรียน

### กจ.2 — บันทึกความ (การแก้ไขความประพฤติ)
- ที่ / วันที่ / เรื่อง: นาย/นางสาว ..... ชั้น ..... เลขที่ ..... เป็นผู้กระทำผิดกฎระเบียบของโรงเรียน
- เชิญ ครูที่ปรึกษา + ผู้ปกครอง + เช็คลิสต์พฤติกรรม (ชุดเดียวกับ กจ.1)
- เพื่อให้ได้เข้ามาพบครูที่ปรึกษาชั้น ..... เพื่อชี้แจงข้อเท็จจริง ในวันที่ ..... ณ ห้อง ..... ของโรงเรียน
- ลงชื่อ ครูที่ปรึกษาชั้น ม.../...
- **การดำเนินการแก้ไขความประพฤติของนักเรียน (เลือกได้หลายข้อ):**
  1. การสอนแนะ
  2. ให้ทำความดีเพื่อชดใช้ความผิด
  3. ให้ช่วยงานตามความสามารถ
  4. ให้เข้าพบงานปรึกษาแนะแนว
- ข้อเสนอแนะเพื่อปรับปรุงแก้ไข (ข้อความ)
- ลงชื่อ ผู้อำนวยการโรงเรียนบาเจาะ

### หลักการ: กรอกครั้งเดียว ใช้ได้ทั้ง 3 เอกสาร (Single Entry)

| ข้อมูล | กรอกที่ไหน | ไหลไปใช้อัตโนมัติที่ |
|---|---|---|
| ชื่อ-สกุลนักเรียน, ชั้น, เลขที่ | ฟอร์มบันทึกเหตุ (ครั้งเดียว) | กจ.1, กจ.1.1, กจ.2 ทุกจุด |
| ชื่อผู้ปกครอง | ฟอร์มบันทึกเหตุ (ครั้งเดียว) | กจ.1 (เชิญ), กจ.1.1 (ลงนาม), กจ.2 (เชิญ) |
| พฤติกรรมที่ผิด (เช็คลิสต์) | ฟอร์มบันทึกเหตุ (ครั้งเดียว) | กจ.1, กจ.2 (checkbox) + กจ.1.1 (รายละเอียด) |
| ครูที่ปรึกษาชั้น | Master data ห้องเรียน (ตั้งครั้งเดียวต่อห้อง) | กจ.1, กจ.2 อัตโนมัติเมื่อเลือกชั้น |
| วัน-เวลา-สถานที่นัดพบ | ตอนออก กจ.1 (ครั้งเดียว) | กจ.2 ดึงมาให้ค่าเริ่มต้น (แก้ได้) |
| เลขที่หนังสือ (ที่ ...) | ระบบรันเลขอัตโนมัติ | กจ.1, กจ.2 |
| ชื่อโรงเรียน, ผอ., รองผอ. | Settings (ตั้งครั้งเดียว) | หัวกระดาษ + ช่องลงนามทุกเอกสาร |
| ตราโรงเรียน | `public/logo-bch.png` | หัวเอกสารทุกแบบ |

→ ครูสารวัตรพิมพ์ข้อมูล **เฉพาะสิ่งที่เปลี่ยนต่อเคสเท่านั้น** (นักเรียน/ผู้ปกครอง/พฤติกรรม/นัดหมาย) ที่เหลือระบบเติมให้หมด

## 4. รายการพฤติกรรม (เช็คลิสต์ใน กจ.1 / กจ.2)

> เก็บเป็น **master data แก้ไขได้ใน Settings** — ยืนยันคำตามต้นฉบับแล้ว:

1. แต่งกายผิดระเบียบบ่อยครั้ง
2. ทำลายทรัพย์สินของผู้อื่น
3. หนีเรียน
4. พกพาอาวุธ
5. ลักขโมย
6. ทำร้ายร่างกายผู้อื่น
7. เล่นการพนัน
8. สิ่งเสพติด (ระบุชนิด) ............
9. ทะเลาะวิวาท
10. ชักนำให้เกิดการทะเลาะวิวาท
11. ชู้สาว
12. อื่น ๆ (ระบุ) ............

## 5. โครงสร้างข้อมูล (Database Schema)

```
users            : id, username, password_hash, name, role
classrooms       : id, level (ป./ม.), grade, room_no, advisor_name
students         : id, full_name, classroom_text (ชั้น), student_no (เลขที่),
                   guardian_name, guardian_phone
                   ← ไม่มีฐานข้อมูลนักเรียนทั้งโรงเรียน: กรอกเฉพาะผู้กระทำผิด
                     ขณะบันทึกเคส ระบบค้นหาชื่อซ้ำอัตโนมัติ → ผูกประวัติเคสเดิม
behaviors        : id, label, is_active, sort_order          ← เช็คลิสต์หัวข้อ 4
incidents        : id, student_id, occurred_at, known_via (เห็นเอง/ได้รับร้องเรียน/ปรากฏ),
                   description, status (recorded/summoned/agreement/corrective/closed),
                   recorded_by, created_at
incident_behaviors : incident_id, behavior_id               ← many-to-many
summons          : id, incident_id, doc_no, meeting_date, meeting_time, meeting_place,
                   printed_at, print_count                   ← กจ.1
agreements       : id, incident_id, behavior_detail, good_deed_text,
                   deadline, signed_at                       ← กจ.1.1
corrections      : id, incident_id, actions (สอนแนะ/ทำความดี/ช่วยงาน/แนะแนว — multi),
                   suggestion, advisor_note                  ← กจ.2
settings         : key-value (ชื่อโรงเรียน, เลขที่หนังสือถัดไป, ชื่อผอ./รองผอ., ปีการศึกษา)
```

## 6. หน้าจอ / ฟีเจอร์

| Route | ฟีเจอร์ |
|---|---|
| `/login` | เข้าสู่ระบบ (ครูสารวัตร) |
| `/dashboard` | สถิติวันนี้/เดือน/ภาคเรียน, เคสค้างดำเนินการ, กราฟตามชั้น/ประเภทพฤติกรรม, นักเรียนเฝ้าระวัง |
| `/incidents/new` | บันทึกเหตุ — ค้นหานักเรียน autocomplete, เลือกพฤติกรรมแบบ chip, วิธีทราบเหตุ |
| `/incidents` | รายการเคส + กรองชั้น/วันที่/สถานะ |
| `/incidents/[id]` | **หน้าเคส** — ไทม์ไลน์สถานะ + ปุ่มพิมพ์ กจ.1 / กจ.1.1 / กจ.2 + บันทึกผลแต่ละขั้น |
| `/print/[docType]/[id]` | หน้าพิมพ์ A4 แบบฟอร์มทางการ (TH Sarabun, ตราโรงเรียน, auto-fill ข้อมูล) |
| `/students` | รายชื่อนักเรียนที่เคยถูกบันทึก (สร้างอัตโนมัติจากเคส) |
| `/students/[id]` | ประวัติเคสทั้งหมดของนักเรียนรายคน |
| `/classes` | จัดการห้องเรียน/ครูที่ปรึกษา |
| `/reports` | รายงานสรุปรายเดือน/ภาคเรียน, export Excel/PDF |
| `/settings` | จัดการรายการพฤติกรรม, ข้อมูลผู้ลงนาม (ผอ./รองผอ.), เลขที่หนังสือ |

## 7. แผนการพัฒนา (Milestones)

1. **M1 – Scaffolding** ✅: init Next.js 16.3.2 + Tailwind v4 + Prisma 6/SQLite + auth (scrypt + HMAC session + proxy guard) + layout ธีมสปอร์ต (Kanit) + login + หน้า placeholder ครบทุก route — build/lint/typecheck ผ่าน
   - บัญชีเริ่มต้น: `admin` / `admin1234` (ควรเปลี่ยนหลังใช้งานจริง)
2. **M2 – Master data** ✅: หน้า /classes (เพิ่ม/ลบห้อง + แก้ครูที่ปรึกษาแบบ inline) + หน้า /settings (ข้อมูลโรงเรียน, ผู้ลงนาม, เลขที่หนังสือถัดไป, รายการพฤติกรรม 12 รายการ — เพิ่ม/แก้/สลับลำดับ/ปิด-เปิด/ลบ, เปลี่ยนรหัสผ่าน) + smoke test ผ่าน 6/6
3. **M3 – Core** ✅: ฟอร์มบันทึกเหตุ (autocomplete นักเรียน + chip พฤติกรรม + snapshot ข้อมูล), รายการเคส (ค้นหา/กรองสถานะ-ห้อง), หน้าเคส (stepper 5 ขั้น + ฟอร์มเวิร์กโฟลว์ กจ.1 → กจ.1.1 → กจ.2 → ปิดเคส + รันเลขหนังสืออัตโนมัติ), หน้านักเรียน + ประวัติรายคน, API ค้นหานักเรียน (มี auth) — smoke test ผ่าน 6/6
4. **M4 – เอกสารทางการ** ✅: หน้าพิมพ์ `/print/[docType]/[id]` — กจ.1 / กจ.1.1 / กจ.2 ตรงต้นฉบับ (A4, ฟอนต์ Sarabun, ตราโรงเรียน, auto-fill ครบ, ✓ ในเช็คลิสต์, ลายเซ็นตาม Settings), ปุ่มพิมพ์ในหน้าเคส (เปิดแท็บใหม่ + autoprint), ป้องกันพิมพ์เอกสารที่ยังไม่ถึงขั้น (404) — smoke test ผ่าน 9/9
5. **M5 – Insight & Output** ✅: แดชบอร์ดสถิติจริง (การ์ดสถิติ, กราฟแท่งตามพฤติกรรม/ห้องเรียน, เคสล่าสุด, นักเรียนเฝ้าระวัง ≥2 เคส) + หน้ารายงาน (กรองเดือน/ปี พ.ศ./ห้อง + สรุป + ตาราง) + export Excel 4 แผ่นงานผ่าน `/api/reports/export` (exceljs) — smoke test ผ่าน 6/6

---

## ✅ โครงการเสร็จสมบูรณ์ทั้ง 5 Milestones (M1–M5)

## 6.1 ฟีเจอร์เสริม: บันทึก PDF เอกสารลงเครื่อง ✅

- หน้าพิมพ์มีปุ่ม **"บันทึก PDF ลงเครื่อง"** → สร้างไฟล์ PDF จริง (A4, ฟอนต์ Sarabun TTF ฝังในไฟล์, ตราโรงเรียน) ผ่าน `@react-pdf/renderer` → `POST /api/docs/save`
- โฟลเดอร์ปลายทางตั้งได้ใน **ตั้งค่า → "โฟลเดอร์เก็บ PDF"** (default `D:\เอกสารกจ`) — สร้างโฟลเดอร์อัตโนมัติถ้ายังไม่มี
- ชื่อไฟล์: `{พฤติกรรมแรก}_{เลขที่หนังสือ}_{ชื่อนักเรียน}_{กจ.X}.pdf` เช่น `ทะเลาะวิวาท_12-2569_นายตัวอย่าง_กจ.1.pdf`
- เชื่อม Google Drive ภายหลัง: ติดตั้ง Google Drive for Desktop แล้วเปลี่ยน savePath ไปที่โฟลเดอร์ในไดรฟ์ sync → ไฟล์ขึ้นเว็บอัตโนมัติ (หรืออัปเกรดเป็น Drive API ได้)
- ป้องกัน: ไม่ล็อกอิน → redirect / เอกสารยังไม่ถึงขั้น → 400 — smoke test ผ่าน 4/4

## 6.2 ฟีเจอร์เสริม: บันไดมาตรการ 4 ระดับ + ลงนามอิเล็กทรอนิกส์ ✅

**บันไดมาตรการตามจำนวนครั้งสะสม (ระบบนับอัตโนมัติ):**
1. ครั้งแรก — ว่ากล่าวตักเตือน
2. ครั้งที่สอง — ทำกิจกรรมสาธารณะประโยชน์ (ทดแทนความดี) + เรียกผู้ปกครองมารับรู้พฤติกรรม
3. ครั้งที่สาม — เชิญผู้ปกครองมาทำทัณฑ์บน
4. ครั้งสุดท้าย — เชิญผู้ปกครองมาเพื่อพิจารณาให้ย้ายสถานศึกษา

- `incident.measureLevel` = จำนวนเคสสะสมของนักเรียน + 1 (สูงสุด 4) — แสดงเป็นแถบสีบนหน้าเคส + ในเอกสาร กจ.2
- **ความเห็นผู้อำนวยการ** (บังคับกรอกทุกเคส) อยู่ในขั้น กจ.2 + พิมพ์ในเอกสาร

**ลงนามอิเล็กทรอนิกส์ 4 ฝ่าย (นักเรียน / ผู้ปกครอง / ครูสารวัตร / ผู้อำนวยการ):**
- วาดลายเซ็นบน canvas (รองรับเมาส์+ทัชสกรีน) → เก็บเป็นภาพ PNG + ชื่อผู้ลงนาม + เวลา
- **ปิดเคสไม่ได้จนกว่าลงนามครบ 4 ฝ่าย** — บังคับทั้ง UI (แสดงขาดใคร) และ server (`closeCase`)
- ลายเซ็น **ปรากฏในเอกสารพิมพ์ HTML และ PDF** (กจ.1.1 ช่องนักเรียน/ผู้ปกครอง/ผอ., กจ.2 ช่องผอ.)
- API: `POST/DELETE /api/signatures`, `POST /api/incidents/close`, `POST /api/incidents` — smoke test ผ่าน 15/15

## 6.3 รูปแบบเอกสาร + กำหนดส่งมาตรการ ✅

**รูปแบบเอกสารพิมพ์/PDF (ตามข้อกำหนดโรงเรียน):**
- ฟอนต์ **TH SarabunPSK ขนาด 16** (PDF ใช้ THSarabun.ttf จาก C:\Windows\Fonts ของเครื่อง มี fallback เป็น Sarabun อัตโนมัติ / HTML print ใช้ฟอนต์ที่ติดตั้งในเครื่อง) — ยกเว้นส่วนหัว (20pt)
- ขอบเอกสาร: **บน 1.5 นิ้ว / ซ้าย 1.5 นิ้ว / ล่าง 1 นิ้ว / ขวา 1 นิ้ว**
- หัวเอกสาร กจ.1 และ กจ.2 ใช้ **ตราครุฑ** (`public/garuda.png`), กจ.1.1 ใช้ตราโรงเรียน (ตามต้นฉบับ)

**กจ.2 — กำหนดวันครบกำหนด:**
- เมื่อติ๊ก "ให้ทำความดีเพื่อชดใช้ความผิด" หรือ "ให้ช่วยงานตามความสามารถ" → ช่องเลือกวันเดือนปีที่ครบกำหนด (บังคับกรอก)
- กำหนดส่งแสดงในหน้าเคสและเอกสาร กจ.2 ("...ภายใน [วันที่]") — เก็บใน correction.actions รูปแบบ JSON `{code, deadline}` (รองรับข้อมูลเก่า)

## 8. รอยืนยันจากเจ้าของโครงการ

- [x] รายการพฤติกรรมหัวข้อ 4 — ยืนยันครบถ้วนแล้ว
- [x] ผู้ลงนาม: ผอ. **ฮาฟีซี อับดุลเลาะ** / รองผอ.ฝ่ายกจน. **มารีนา อารง** (แก้ได้ใน Settings)
- [x] ตราโรงเรียน — คัดลอกเป็น `public/logo-bch.png` แล้ว
- [x] นักเรียน: กรอกเฉพาะผู้กระทำผิดขณะบันทึกเคส (ไม่ import ทั้งโรงเรียน)
